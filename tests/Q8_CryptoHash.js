
/**
 * Real test Q8 final
 *
 *Output hash
If you use a document as the input of the hashing function,
 what can you do with the output hash? This question has more than one answer

 
  Pick one of the choices:

a: Compare whether you have the same document as someone else without comparing the documents themselves (CORRECT)
b: Send the hash to another party for them to decrypt the hash to retrieve the document
c: Create a table where anyone who has the hash can find the document (CORRECT)
d: Compress the document content so it can be extracted later

 */
